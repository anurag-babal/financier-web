import logo from '../assets/logo.svg';

export default function Header() {
  return (
    <header>
      <h1>My Header</h1>
      console.log('Header');
      {console.log('Header')}
      {/* <img src={logo} alt='logo' /> */}
    </header>
  );
}